# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/

# Build output of `just check`: the BDD traceability report (tools/canon_bdd).
reports/

# uv.lock is committed on purpose: it is authoritative for CI, the Coolify image
# build and a developer's machine.

# Node
node_modules/
.svelte-kit/
build/

# CyberCanon local state (rebuildable)
.canon/index.sqlite
.canon/queries.log
# Reported validation outcomes waiting for delivery (add-mcp-writes, D6). A
# retry buffer, never canon: the verdict is authoritative locally and the
# outcome's durable home is the committed `asset.validation.json` (G2).
.canon/reports.ndjson

# Environment
.env
.env.local
