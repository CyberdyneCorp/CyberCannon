These decoder files are copied from `three@0.180.0/examples/jsm/libs/draco/gltf`.
They are served by the web app so validated Draco-compressed previews can be
decoded without requesting code from an external CDN. Update them together with
the pinned `three` dependency.
