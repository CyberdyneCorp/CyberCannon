# fix-web-oidc-discovery

Browser sign-in against the real CyberdyneAuth: endpoints from discovery, a same-origin token relay, refresh-token renewal, identity-provider sign-out, and the acting identity named from the id token
