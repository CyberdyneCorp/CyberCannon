# Tasks

## 1. Unknown is not unmapped

- [x] 1.1 Give the session identity a `mapping` of `mapped`, `unmapped` or `unknown`, from whether the credential carries `git_emails` and whether it names anyone; verify a CyberdyneAuth-shaped credential is `unknown`
- [x] 1.2 Raise the unmapped warning for `unmapped` only; verify a CyberdyneAuth-shaped session renders no warning, and a credential carrying an empty `git_emails` still does
