# add-web-backend

FastAPI HTTP surface, CyberdyneAuth OIDC integration, hosted repository access via persistent working copy with direct-commit write-back, PostgreSQL index, MinIO blobs, and asset request workflow
