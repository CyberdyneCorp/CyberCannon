# add-mcp-read-server

Local stdio FastMCP read surface for agents: where_is, list_assets, get_asset_spec with discipline lenses, get_constraints, diff_spec, validate_export; identity from credential, never from a tool parameter
