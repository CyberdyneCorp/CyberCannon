# Tasks

## 1. Project navigation

- [x] 1.1 Add Assets and Triage links to the shared project frame with a visible and accessible current state; verify links retain the project and clear destination-specific filters.

## 2. Asset navigation

- [x] 2.1 Render the asset's Overview, Model sheet, and 3D viewer links on every asset surface with a visible and accessible current state; verify the Overview has one copy of each link.

## 3. Verification

- [x] 3.1 Verify navigation behavior and narrow-screen rendering with focused tests, then run the web type check, adherence check, and OpenSpec validation.
